# AGENTS.md

> Cross-tool entry point for the AI development workflow installed from `ai-workflow-portable/`.
> OpenCode, Codex, and Cursor read this file directly. Claude Code reads `CLAUDE.md` instead —
> it is a required, full mirror of this file's content (not optional), kept in sync by `/setup`.
>
> **On first setup:** if `.project/` does not exist yet, read `SETUP.md` and run the install
> steps. Do this automatically as soon as it's detected — don't wait to be asked. After setup,
> extend this file's "Project specifics" section with what you learned, and make sure `CLAUDE.md`
> reflects the same specifics.

## What this project uses

A ShapeUp-style, 5-phase AI development pipeline with specialized subagents, coding rules,
a persistent knowledge graph, and cost-aware model routing.

- **Pipeline & gates:** `ai-framework/workflow/overview.md` (read this first)
- **Harness adapters & model profiles:** `ai-framework/integrations/harnesses.md`
- **Coding/security/architecture rules:** `ai-framework/rules/` (stack-agnostic, search don't bulk-load)
- **Stack-specific rule companions:** `.project/rules/` (generated by `/setup` from your actual stack)
- **Model routing:** `ai-framework/rules/model-routing.md`
- **Per-project data:** `.project/` (context, rules, knowledge, design, requirements, pitches, runs, status)
- **Knowledge graph:** `.project/knowledge/graph.json` (nodes/edges/tag-index, traversable) +
  `index.md` (human-readable) — both generated by `node ai-framework/scripts/graphify.js`, never
  hand-edited. Rebuild after any edit to an entry under `.project/knowledge/`.

## The pipeline

```
(brainstorm) → /shape → /critique → bet → /plan → /build → /audit → /ship
                                                            every 5 ships → /cooldown
```

| Phase | Playbook | Purpose |
|-------|----------|---------|
| shape | `.claude/skills/shape/SKILL.md` | Frame problem, set appetite, name rabbit holes, no-gos |
| critique | `.claude/skills/critique/SKILL.md` | Pre-bet red-team (big-batch + AI scopes) |
| plan | `.claude/skills/plan/SKILL.md` | Decompose into independent scopes with checkable exits |
| build | `.claude/skills/build/SKILL.md` | Execute scopes, hill-tracked, verify before done |
| audit | `.claude/skills/audit/SKILL.md` | Parallel review/security/test/ux/i18n/eval fan-out |
| ship | `.claude/skills/ship/SKILL.md` | Reconcile pitch↔impl, extract knowledge, compact status |
| cooldown | `.claude/skills/cooldown/SKILL.md` | Promote issue→pattern→rule, prune, triage backlog |

| Harness | Native phase entry points | Native role adapters |
|---------|---------------------------|----------------------|
| Claude Code | `.claude/skills/` | `.claude/agents/` |
| OpenCode | `.opencode/commands/` and `.claude/skills/` | `.opencode/agents/` |
| Codex | `.agents/skills/` | `.codex/agents/` |

When a harness lacks native commands or dispatch, open the matching phase `SKILL.md` and run
constrained role passes sequentially. Never skip gates or evidence requirements because an
integration capability is unavailable.

## Subagents

`.claude/agents/*.md` are canonical role prompts. Claude Code uses them directly; OpenCode and
Codex use native adapters that load the canonical prompt. Select the available model for the
role's `fast`, `standard`, or `deep` profile in `ai-framework/integrations/harnesses.md`.
Key roles: `security-reviewer`, `code-reviewer`, `test-coverage-checker`,
`ux-reviewer`, `i18n-checker`, `eval-runner`, `skeptic`, `architect`, `planner`,
`build-error-resolver`, `refactor-cleaner`.

**Nested dispatch:** any phase or agent may spawn further sub-agents for independent subtasks —
not just the fan-outs already named in `/critique` and `/audit` — when the active harness
supports it, and pick each spawned subtask's model by its own complexity, not the parent's
profile. Falls back to a sequential pass on a harness without nested dispatch. See "Sub-agent
Dispatch" in `ai-framework/integrations/harnesses.md` for the per-harness capability table and
the model-selection rule.

## Confirmation gate (always)

Every gated phase ends with **Approve / Revise / Back / Stop**. Never auto-advance. The
adaptive gate matrix in `ai-framework/workflow/overview.md` says which gates apply to which
appetite (small-batch / big-batch / bug-fix / hotfix).

## Response style (caveman mode)

Default response style is brevity (`caveman`, default level `full`). If the `caveman` skill is
installed and enabled (`.project/skills/registry.json`), load it at the level
`node ai-framework/scripts/skill-defaults.js resolve-mode --phase agent --args-text "<the user's message>"`
returns, unless that is `off`. A `caveman=<lite|full|ultra|wenyan-lite|wenyan-full|wenyan-ultra|off>`
token in a message overrides it for that message only; a persistent change is an explicit edit to
`.project/skills/modes.json`. Skills, phase commands, and subagents apply the same resolution
themselves (`ai-framework/integrations/skill-defaults.md`). The skill's own Auto-Clarity and
Boundaries rules still apply: security warnings, irreversible-action confirmations, and text
persisted to project files stay normal prose.

## Guardrails

- Never read/commit secret files (`.env`, `.env.local`, `credentials.json`, `settings.local.json`).
- Never rewrite historical records under `.project/pitches/`, `.project/design/`, `_archive/`.
- Load only what the current phase needs; traverse `.project/knowledge/graph.json` instead of
  bulk-loading every file under `knowledge/`.

---

## Project specifics

### Product

La Salle Norandino is defining a single-domain, multi-site public platform for district, country,
territory, and school contexts. The active shaped pitch is
`.project/pitches/district-multisite-foundation/`.

### Architecture Direction

- Tenant hierarchy: `district -> country -> territory (optional) -> school`.
- Users and administrators must be restricted to their assigned organisation node and permitted
  descendants; tenant checks are server-enforced.
- Canonical path-based sites, configurable but controlled landing sections, scoped articles/blog,
  media/documents, and migration with redirects are in scope.
- Data provider, authentication, CMS/editor, hosting, infrastructure, and operations are
  intentionally undecided pending discovery.

### Technology Direction

- Proposed only: TypeScript, TanStack Start and justified TanStack packages, Tailwind, Vite, and
  Ox tooling as needed.
- No application dependencies, package manager, scripts, or infrastructure have been configured.

### Project Records

- Public-site evidence: `.project/analysis/2026-09-21-current-site-validation.md`.
- Requirements: `.project/requirements/features/multisite-platform.md`.
- Scope and effort estimate: `.project/pitches/district-multisite-foundation/pitch.md`.
- First discovery scope: organisation/source inventory, access governance, migration design, then a
  technical proposition. Do not begin implementation until the pitch has a recorded bet decision.

### Verification

- Validate workflow records: `node ai-framework/scripts/setup-validator.js`.
- Validate the knowledge graph: `node ai-framework/scripts/graphify.js --check`.

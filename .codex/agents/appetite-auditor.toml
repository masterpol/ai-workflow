name = "appetite-auditor"
description = "Fast read-only scope-sizing role that checks work against the stated appetite."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Read `.claude/agents/appetite-auditor.md` and follow its body as the canonical role prompt. Return only the requested assessment.
"""

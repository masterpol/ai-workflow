{
  "description": "Records a bounded local token-consumption snapshot after every Codex subagent.",
  "hooks": {
    "SubagentStop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"$(git rev-parse --show-toplevel)/ai-framework/hooks/scripts/token-consumption.js\" --vendor codex --event subagent-complete --root \"$(git rev-parse --show-toplevel)\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}

# Done Work

Compact record of shipped pitches whose full history was compacted by `/pitch-compress`. Each
entry is a short summary, a pointer to the durable knowledge it produced, and a pointer to its
immutable recovery archive. Never hand-edit — regenerated by
`node ai-framework/scripts/pitch-compress.js write-done-work`.

## portable-skill-installation — shipped 2026-09-25

Tracked skill installation shipped as bundle 2.4.0 in three sequential scopes: S1 the verified, transactional
installer (`add-skill.js`, `skill-registry.js`, `skill-source.js`, `skill-vendors.js`; exact Git-object
resolution, symlink/traversal/secret-name rejection, journal + lock + crash recovery, project and global
scope); S2 native `add-skill` entry points for Claude Code, Codex, OpenCode and a generated Cursor mirror,
with the doctor and setup-validator recognising registry-owned skills; S3 registry-owned paths excluded from
`bundle-sync` comparison, `skill-sync.js reconcile` re-rendering drifted wrappers, and optional Prettier
formatting with dual-hash tracking. 63 tests, 100% line coverage on the four `skill-*.js` modules.

No-gos held: nothing was installed or deleted during shaping or build, instance customization was never
overridden (locally modified owned files report `conflict`), and the secret-filename pattern is enforced on
fetch and on every write path. Audit cycle 1 had zero must-fix: one should-fix fixed (YAML quoted-scalar
unescaping in `skill-source.js`, with regression tests), one deferred (an ancestor-symlink TOCTOU race that
needs a co-resident writer), one note closed by a regression test showing the formatter's `--config` only ever
resolves from the trusted project root. The downstream pitches (defaults, compaction, metrics, state report)
were checked against what was built and all four have since shipped on this registry.

Lessons live in [[dual-hash-tracking-for-transformed-content]], [[subprocess-revalidation-against-on-disk-code]],
[[resolve-config-only-from-trusted-root]], [[macos-tmpdir-realpath-alias-breaks-path-assertions]] and
[[workflow-tooling-pitches-share-standing-no-gos-and-design-answers]]; open followups (the TOCTOU race and a
`bundle-sync` marker quirk) are in `.project/pitches/_followups.md`.

Recovery archive (byte-for-byte, checksummed): `.project/compaction/archives/portable-skill-installation-2026-09-25T13-27-02-414Z/`; restore with `node ai-framework/scripts/pitch-archive.js restore portable-skill-installation --apply`. Ship-time log: `.project/runs/2026-09-25-portable-skill-installation.md`. Coverage ledger: `.project/compaction/ledgers/portable-skill-installation.json` (19 of 19 sections extracted, 0 gaps).

## portable-skill-defaults — shipped 2026-09-25

Configurable default skills shipped in three scopes: D1 a proposal-only catalog (`skill-defaults.json`),
`skill-defaults.js` mode precedence (invocation > phase override > instance default > bundle default) carried
by one paragraph in all seven phase skills, and a doctor check on the catalog; D2 mode attribution on
`token-consumption.js` records plus `skill-compress-guard.js`, which blocks `caveman-compress` from this
project's protected historical records; D3 `browser-runtime.js`, an `agent-browser` readiness report that never
installs. 108 tests across the skill surface (44 new or changed), 100% line, branch and function coverage on the
three new scripts.

No-gos held: nothing was installed during shaping or build, `.project/skills/modes.json` is never touched by
`bundle-sync` (regression-tested), and the `mode` field is a plain attribution string with no savings claims.
Audit cycle 1: 3 must-fix (a symlink bypass and a case-folding bypass in the compress guard's path matching, both
reproduced live before and after the fix, plus a missing automated test for the doctor check) and 2 should-fix (an
ancestor-directory symlink bypass in mode-file loading; a missing subprocess timeout), all fixed, none left open.
No new followups; downstream pitches (compaction, metrics, state) were checked against what was built.

Design answers are in [[skill-defaults-attribution-guard-and-runtime-design]] and the standing constraints in
[[workflow-tooling-pitches-share-standing-no-gos-and-design-answers]]. The lesson from the two bypasses is
[[resolve-before-matching-a-protected-path-allowlist]]; the default-mode decision is [[caveman-mode-is-default-everywhere]].
Bundled in CHANGELOG 2.5.0.

Recovery archive (byte-for-byte, checksummed): `.project/compaction/archives/portable-skill-defaults-2026-09-25T13-39-03-844Z/`; restore with `node ai-framework/scripts/pitch-archive.js restore portable-skill-defaults --apply`. Ship-time log: `.project/runs/2026-09-25-portable-skill-defaults.md`. Coverage ledger: `.project/compaction/ledgers/portable-skill-defaults.json` (16 of 16 sections extracted, 0 gaps).

## pitch-compaction — shipped 2026-09-25

Recoverable pitch compaction shipped in two scopes: C1 the `/pitch-compress` skill (all vendors) with
`pitch-compress.js` (`inventory`, `ledger`, `commit-ledger`, `write-done-work`) and the `done-work.md` scaffold;
C2 `pitch-archive.js` (`archive`, `verify`, `remove`, `restore`, `recover`) reusing the audited transaction
machinery, with real SIGKILL interruption tests. 40 tests for the pitch (151 across the skill surface), 100%
line, branch and function coverage.

Confidence caveat kept on purpose: the audit was only partly independent. Only the code-reviewer completed; the
security, coverage and cross-pitch reviewers died on a provider rate limit, so the author ran the highest-risk
attack scenarios directly. That closed four must-fix defects (an archive lookup that crossed between pitches
sharing a slug prefix; `restore` trusting an unverified, tamperable archive; a ledger destination inside the pitch
being deleted; a ledger whose author declared every section a gap passing the deletion gate at coverage 0) and two
should-fix. An independent re-review is a followup, and this tool's first real use is recorded there too.

No-gos held: every mutating step needs `--apply`, deletion needs a verified archive, a complete ledger with each
gap explicitly accepted, an unchanged source, and a human gate; nothing was archived or deleted during the build.
The design is in [[pitch-compaction-gate-and-recovery-design]]; lessons in [[a-gate-must-not-trust-its-own-author]]
and [[bare-prefix-match-crosses-entities]]; constraints in
[[workflow-tooling-pitches-share-standing-no-gos-and-design-answers]]. `project-state-report` reads this file as a
generated, read-only source. No CHANGELOG entry was ever written for the feature itself (only the 2.8.0 hardening entry
mentions `pitch-compress`); see the followup on that.

Recovery archive (byte-for-byte, checksummed): `.project/compaction/archives/pitch-compaction-2026-09-25T13-39-04-109Z/`; restore with `node ai-framework/scripts/pitch-archive.js restore pitch-compaction --apply`. Ship-time log: `.project/runs/2026-09-25-pitch-compaction.md`. Coverage ledger: `.project/compaction/ledgers/pitch-compaction.json` (15 of 15 sections extracted, 0 gaps).

## metrics-report-dimensions — shipped 2026-09-25

Metrics report dimensions shipped in five scopes (S0 payload spike, S1 collector schema v2, S2 renderer, S3
harness feeders, S4 docs/doctor/end-to-end): `.project/metrics/token-consumption.md` and `.html` now open with a
Usage summary (most used vendor and model and the basis it was ranked on) followed by tables by vendor, model,
agent, reasoning effort and skill. Values a harness does not report show `Unreported`; completions with no nonzero
cost show `Unpriced`; counts start at the v2 migration date and nothing is back-filled. 76 tests (collector,
renderer, OpenCode plugin, skill-defaults); collector 98.9%, renderer 100%, plugin 92.3% line coverage.

Audit, two cycles: 2 must-fix (no automated tests for the CLI or the OpenCode plugin) and 5 should-fix security
items (Markdown injection through names, a symlinked metrics directory, unbounded ids and status, float underflow,
smaller ones), all fixed, each reproduced first; cycle 2 found a further gap (agent ids shown as labels), also
fixed. Caveat kept on purpose: the cycle-2 security re-check was dispatched three times and never returned, so it
was author-run, and the user approved shipping on that basis; an independent re-review is a followup, as are the
collector lock PID-reuse flaw, identity-key collisions, and live verification of the OpenCode and Claude hook
wiring. Process notes for cooldown (fast-profile code review found nothing while the standard security review
found seven real issues; a mutation pass caught a test that did not test its guard; three reviewer dispatches
failed) became the "Guards and Fixes" rule in `ai-framework/rules/testing.md`.

Design and no-gos: [[token-metrics-dimensions-design]]. Lessons: [[reversible-aggregates-store-their-routing]],
[[allow-list-untrusted-labels-at-ingest-and-at-render]], [[async-agent-launch-hook-counted-as-completion]].
Bundled in CHANGELOG 2.6.0.

Recovery archive (byte-for-byte, checksummed): `.project/compaction/archives/metrics-report-dimensions-2026-09-25T13-39-04-339Z/`; restore with `node ai-framework/scripts/pitch-archive.js restore metrics-report-dimensions --apply`. Ship-time log: `.project/runs/2026-09-25-metrics-report-dimensions.md`. Coverage ledger: `.project/compaction/ledgers/metrics-report-dimensions.json` (17 of 17 sections extracted, 0 gaps).

## project-state-report — shipped 2026-09-25

`/state` shipped in two scopes: T1 the fact collector and skill entry points (`state-snapshot.js` writing
`.project/reports/state.json`, schema v1, every fact `{value, status, evidence, note?}`), T2 theme discovery and the
self-contained HTML report (`state-theme.js`, `state-render.js`; strict theme grammar, contrast-verified fallback,
escaped output, safe links only, `settings.json` surviving sync). 87 tests for the pitch (270 across the whole
surface); the three new scripts at 99.9% lines and 93% branches. Claude Code and OpenCode verified live; Codex and
Cursor entry points are unverified in a running host (followup).

Audit ran three cycles with 0 must-fix left open. Cycle 1: 4 must-fix (quadratic regexes on project markdown,
executing a project-supplied `skill-sync.js`, an appetite regex crossing lines, an untested read bound) and 11
should-fix; cycle 2: one regression a cycle-1 fix introduced (`write-done-work` following a symlinked
`done-work.md`); cycle 3: two should-fix. Reviewer reliability was poor (rate limits, stalls, a coverage report
contradicted by measurement) and every claim was re-verified by the author. The secret scrub is best-effort and
documented as such. `pitch-compress.js` was hardened as part of this pitch.

Design: [[project-state-report-design]]. Lessons: [[parse-untrusted-values-and-re-emit-them]],
[[a-report-over-an-untrusted-tree-runs-only-bundle-code]], [[hardening-a-shared-reader-made-its-writer-destructive]],
[[reviewer-reports-contradicted-by-measurement]]. Bundled in CHANGELOG 2.8.0; the first cooldown promoted two rules
from it (`security.md` section 9, `testing.md` Guards and Fixes).

Recovery archive (byte-for-byte, checksummed): `.project/compaction/archives/project-state-report-2026-09-25T13-39-04-583Z/`; restore with `node ai-framework/scripts/pitch-archive.js restore project-state-report --apply`. Ship-time log: `.project/runs/2026-09-25-project-state-report.md`. Coverage ledger: `.project/compaction/ledgers/project-state-report.json` (20 of 20 sections extracted, 0 gaps).

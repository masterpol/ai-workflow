# Cooldown 2026-09-25

**Trigger:** 5 ships since the last cooldown (portable-skill-installation, portable-skill-defaults,
pitch-compaction, metrics-report-dimensions, project-state-report). First cooldown; no earlier report.
Every action below was approved individually.

## 1. Knowledge health

`graphify.js --check`: 17 entries (9 patterns, 7 issues, 1 decision), 35 edges, 0 broken links, 0 orphans.
All entries are dated 2026-09-25, so nothing is stale (60+ days) and nothing qualifies for `_archive/`.
No entry carries a confidence field yet; the templates do not require one. Parked pitches: none. Archive: empty.

## 2. Issue → pattern → rule promotion

Test applied: the same issue class in at least two pitches' audits or deviations, even if resolved.

| Class | Pitches | Sources | Decision |
|---|---|---|---|
| Path / symlink / untrusted-input must-fix findings | all 5 (installation, defaults, compaction, metrics, state-report) | patterns `a-report-over-an-untrusted-tree-runs-only-bundle-code`, `resolve-before-matching-a-protected-path-allowlist`, `resolve-config-only-from-trusted-root`, `parse-untrusted-values-and-re-emit-them`, `allow-list-untrusted-labels-at-ingest-and-at-render`; issue `hardening-a-shared-reader-made-its-writer-destructive` | **Promoted** → `ai-framework/rules/security.md` section 9 + checklist G |
| Guard untested until mutated; a fix needed its own re-review; reviewer claims contradicted | 4 (defaults, compaction, metrics, state-report) | issues `reviewer-reports-contradicted-by-measurement`, `hardening-a-shared-reader-made-its-writer-destructive`; pattern `a-gate-must-not-trust-its-own-author` | **Promoted** → `ai-framework/rules/testing.md` "Guards and Fixes: Prove the Test Can Fail" + 2 checklist lines |

Not promoted (single pitch each, or not a rule): `dual-hash-tracking-for-transformed-content`,
`subprocess-revalidation-against-on-disk-code`, `reversible-aggregates-store-their-routing`,
`bare-prefix-match-crosses-entities`, `macos-tmpdir-realpath-alias-breaks-path-assertions`,
`prose-instructions-must-specify-how-to-extract-from-free-form-arguments`,
`installed-skill-wrappers-ship-as-orphans`, `async-agent-launch-hook-counted-as-completion`, decision `caveman-mode-is-default-everywhere`.
They stay as knowledge; reconsider when a second pitch repeats them.

## 3. Parked pitches

None.

## 4. Followups triage (14 items)

Bundled into candidate pitches (approved): `independent-rereview-catch-up` (3 items), `path-safety-hardening` (2),
`live-host-verification` (3), `collector-robustness` (2). Standalones promoted (approved): `bundle-sync-marker-fix`,
`state-quoted-fonts`, and the first real `/pitch-compress` trial (with the "wire /state in" decision folded into it).
Nothing discarded; no item has expired. Details are at the top of `.project/pitches/_followups.md`.

## Observations for the next cycle (no action taken)

- Three of five audits lost reviewers to a provider rate limit; two stalled on the watchdog. The rules now say what
  to do (hard limits, verify claims, re-review fixes), but `/audit` itself does not enforce a budget. Part of
  `independent-rereview-catch-up` should decide whether the audit skill needs a reviewer-completion contract.
- `.project/status.md` still lists three "open rabbit holes" that belonged to the district pitch, which was removed
  from the working tree by another session. Not touched here.
- Nothing is committed: this session's work and the other session's `metrics-report-dimensions` work are both in
  the working tree. The version bump belongs to the commit.

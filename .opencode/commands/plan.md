---
description: Turn a bet pitch into an executable plan.
model: openai/gpt-5.6-terra
---

Load `.claude/skills/plan/SKILL.md` and follow it exactly. This command uses `standard` for small-batch work; dispatch the `planner` subagent for big-batch architecture and parallel-dispatch decisions. $ARGUMENTS

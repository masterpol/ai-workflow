---
description: Audit project dependencies for known vulnerabilities and security-relevant updates without changing manifests or lockfiles.
model: openai/gpt-5.6-terra
---

Load `.claude/skills/dependency-security/SKILL.md` and follow it exactly. Do not update dependencies, run an audit-fix command, install audit tooling, or rewrite a lockfile without explicit user approval. $ARGUMENTS

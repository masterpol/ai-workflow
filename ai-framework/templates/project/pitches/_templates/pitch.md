# Pitch: {slug}

**Date**: YYYY-MM-DD  •  **Appetite**: small-batch | big-batch (epics must decompose first)
**Stack**: web | mobile | backend | ai-prompts | (combo)

## Problem

[1-2 sentences, user-centric. "Teachers can't see X across runs" — not "we should build a comparison view".]

## Knowledge consulted

(Mandatory — searched before writing the rest of the pitch.)

- [issues/...] — relevance
- [patterns/...] — applies / reusable
- [decisions/...] — constrains

## Solution sketch (breadboard, NOT wireframe)

**Places**: {list of screens / services / files-as-surfaces}

**Affordances per place**: {list of buttons / fields / calls / mutations}

**Connections**: {how user / data flows between places}

For UI: places + affordances table or fat-marker ASCII. No visual layout — that's /plan.
For backend: operation sequence + data touchpoints (which queries, which schema fields).
For AI/prompt: prompt key + I/O shape + success criterion + ≥1 golden eval case at `.project/evals/datasets/`.

## Rabbit holes

**Resolved here** (with answer):
- [unknown] → resolution: {answer}

**Pushed to /plan as risk** (with named owner):
- [unknown] — flagged as plan-time risk; spike if needed

**Pushed to no-go** (deferred):
- [unknown] — out of this pitch's appetite

## No-gos (this pitch)

- ✗ {explicit out-of-scope item}
- ✗ {another}

## Critique findings (auto-populated by /critique for big-batch + AI scopes)

(Empty until /critique runs. Each finding has severity / type / suggested addition.)

## Bet decision

☐ **Bet** (→ /plan)  
☐ Re-shape — named gap: {which rabbit hole un-resolved? which critique finding?}  
☐ Pass — moved to `.project/pitches/_parked/{slug}/`; reason: {…}

If acknowledging critique findings without addressing, log reason here.

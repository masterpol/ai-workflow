---
name: setup-validator
description: Verify that workflow setup generated complete project context, entry files, knowledge graph, and harness mirrors.
---

# Setup Validator

Run `node ai-framework/scripts/setup-validator.js` from the target project root. It is read-only;
re-run `/setup` through its confirmation gates if the validator reports failures.

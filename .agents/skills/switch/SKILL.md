---
name: switch
description: Switch between pitches. Checkpoints the current pitch, then loads the target pitch's context via the /resume flow.
---

# switch

Load `.claude/skills/switch/SKILL.md` and follow it exactly. Use the `fast` capability profile from `ai-framework/integrations/harnesses.md`.

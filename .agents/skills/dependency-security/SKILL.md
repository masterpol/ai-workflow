---
name: dependency-security
description: Audit project dependencies for known vulnerabilities and security-relevant updates without changing manifests or lockfiles.
---

# Dependency Security

Load `.claude/skills/dependency-security/SKILL.md` and follow it exactly. Do not update dependencies, run an audit-fix command, install audit tooling, or rewrite a lockfile without explicit user approval.
